Implemente uma função calcular_area_retangulo(largura, altura) que receba a largura e altura de um retângulo e retorne sua área. 
Teste a função solicitando os valores ao usuário.
'''

def calcular_area_retangulo(largura, altura):
    return largura * altura

largura = float(input("Digite a largura do retângulo: "))
altura = float(input("Digite a altura do retângulo: "))

area = calcular_area_retangulo(largura, altura)

print(f"a largura do retangula é: {area}")
